# Server Configuration
server.port=8080

# MongoDB Configuration
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=amazin_bookstore

# Logging
logging.level.com.amazin.bookstore=DEBUG
logging.level.org.springframework.data.mongodb=DEBUG

# Application Name
spring.application.name=amazin-bookstore
